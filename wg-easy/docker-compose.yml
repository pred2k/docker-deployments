volumes:
  etc_wireguard:

services:
  wg-easy:
    labels:
      caddy: https://wg-easy.$DOMAIN
      caddy.reverse_proxy: "{{ upstreams 51821 }}"
      caddy.import_0: tls
      caddy.import_1: security-headers
      caddy.import_2: tinyauth_forwarder *

      tinyauth.apps.wg-easy.config.domain: wg-easy.$DOMAIN
      tinyauth.apps.wg-easy.oauth.groups: wg_easy  # group from pocket-id
    environment:
    #  Optional:
    #  - PORT=51821
    #  - HOST=0.0.0.0
      - INIT_ENABLED=true
    # dont expose the web-ui. Access via SSH tunnel only.
      - INSECURE=false
    # use dnsforge.de DNS servers
      - INIT_DNS=49.12.67.122,2a01:4f8:c013:29d::122

    image: ghcr.io/wg-easy/wg-easy:15
    container_name: wg-easy
    networks:
      wg:
        ipv4_address: 10.42.42.42
        ipv6_address: fdcc:ad94:bacf:61a3::2a
      caddy:
    volumes:
      - etc_wireguard:/etc/wireguard
      - /lib/modules:/lib/modules:ro
    ports:
      - "51820:51820/udp"
      - "51821:51821/tcp"  # Web-UI
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
      # - NET_RAW # ⚠️ Uncomment if using Podman
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv6.conf.all.disable_ipv6=0
      - net.ipv6.conf.all.forwarding=1
      - net.ipv6.conf.default.forwarding=1

networks:
  wg:
    driver: bridge
    enable_ipv6: true
    ipam:
      driver: default
      config:
        - subnet: 10.42.42.0/24
        - subnet: fdcc:ad94:bacf:61a3::/64
  caddy:
    external: true
